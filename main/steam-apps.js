// Steam app list cache + autocomplete search — stub for step 1
// Real implementation in step 8

function search(query) {
  console.log('[steam-apps] search stub called with query:', query);
  return [];
}

module.exports = { search };
